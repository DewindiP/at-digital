import React from 'react'

// This is just for the text styling
export default function ButtonText({ text }) {
  return (
    <span style={{ color: "#fff", fontSize: "14px", fontWeight: "bold" }}>
      {text}
    </span>
  );
}


